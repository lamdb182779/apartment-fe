"use client"

import {
    Accordion,
    AccordionContent,
    AccordionItem,
    AccordionTrigger,
} from "@/components/ui/accordion"

export default function Apartment({ number }: { number: number }) {


    return (
        <div>

        </div>
    )
}